# Tension — Enterprise AI Canvas Platform

> **"Tension is the space between what you know and what you discover."**

---

## 🎯 Миссия

Tension — это **IDE для нелинейного мышления** с использованием LLM. В отличие от традиционных чат-интерфейсов, которые загоняют пользователя в "туннель" контекста, Tension позволяет исследовать **пространство вариантов** через визуальный граф диалогов.

**Ключевая идея:** Мы не строим "диалог", мы строим **"карту мыслей"**.

---

## 📊 Текущий статус: v0.9 (Enterprise Beta)

### Архитектура

```
src/
├── app/                    # Точка входа, глобальные стили
│   └── styles/             # Единая CSS система
├── entities/               # Бизнес-сущности (FSD Layer 3)
│   ├── canvas/             # Состояние холста (zoom, offset, tool)
│   ├── chat/               # Модель чата
│   ├── node/               # Модель ноды (prompt, response, branches)
│   ├── settings/           # Настройки приложения
│   └── thread/             # Модель треда
├── features/               # Фичи (FSD Layer 4)
├── pages/                  # Страницы (FSD Layer 5)
│   └── workspace/          # Главная рабочая область
│       ├── model/          # useWorkspaceModel — центральная логика
│       └── ui/             # WorkspacePage компонент
├── shared/                 # Переиспользуемый код (FSD Layer 1)
│   ├── config/             # Константы (NODE_WIDTH, ZOOM_MIN, etc.)
│   ├── db/                 # IndexedDB (tensionDb)
│   ├── lib/                # Хуки, контексты, утилиты
│   └── ui/                 # UI компоненты, токены, иконки
└── widgets/                # Композитные виджеты (FSD Layer 6)
    ├── canvas/             # Canvas + NodeCard
    ├── command-palette/    # Cmd+K палитра команд
    ├── context-menu/       # Контекстное меню
    ├── minimap/            # Миникарта с zoom controls
    ├── settings-panel/     # Панель настроек
    ├── sidebar/            # Боковая панель с чатами
    └── toolbar/            # Панель инструментов
```

### Технологический стек

| Категория | Технология |
|-----------|------------|
| **Framework** | React 18 + TypeScript 5 |
| **Build** | Vite 5 |
| **Architecture** | Feature-Sliced Design (FSD) |
| **Styling** | CSS Variables (Design Tokens) |
| **Icons** | Lucide React |
| **Storage** | IndexedDB (Dexie.js) |
| **AI** | OpenAI API (GPT-4, GPT-4o, etc.) |

---

## ✅ Реализованный функционал

### 🖼️ Canvas System
- **Бесконечный холст** — неограниченное пространство для работы
- **Zoom** — масштабирование 25%–200% (Cmd+Scroll, кнопки в minimap)
- **Pan** — панорамирование (Hand tool, Space+Drag, Cmd+Drag)
- **Tool modes** — Cursor (выбор) и Hand (перемещение)
- **Grid dots** — адаптивная сетка под тему (чёрные/белые точки)

### 🧩 Node System
- **Создание нод** — автоматическое при ветвлении
- **Drag & Drop** — перетаскивание нод по холсту
- **Ветвление** — 1-4 параллельных ветки от каждой ноды
- **Deep Level** — рекурсивная генерация 1-4 уровня вглубь
- **Collapsible sections** — сворачивание длинного контента
  - Контекст (запрос): 2 строки
  - Ответ модели: 10 строк
  - Новый запрос: 3 строки
- **Play button** — запуск генерации с визуальным feedback
- **Selection** — выделение нод с обводкой (чёрная/белая по теме)

### 🎨 Design System
- **Theme support** — полная поддержка Light/Dark тем
- **Design Tokens** — 100+ CSS переменных для консистентности
- **Semantic colors** — primary, success, warning, error
- **Response highlighting** — зелёная подкладка для ответов модели
- **Enterprise typography** — Inter font, продуманная иерархия

### 🗂️ Data Management
- **IndexedDB** — локальное хранение всех данных
- **Multi-chat** — множество независимых диалогов
- **Auto-save** — автоматическое сохранение изменений
- **Export/Import** — JSON формат для переноса данных
- **Undo/Redo** — история изменений графа

### 🎛️ UI Components
- **Toolbar** — инструменты, undo/redo, export/import, save status
- **Minimap** — обзор графа + zoom controls + viewport indicator
- **Sidebar** — список чатов, создание новых
- **Command Palette** — Cmd+K для быстрых действий
- **Context Menu** — правый клик для действий с нодами
- **Settings Panel** — API ключ, выбор модели, тема
- **Toast notifications** — уведомления об ошибках и успехе

### ⌨️ Keyboard Shortcuts
| Shortcut | Action |
|----------|--------|
| `V` | Cursor tool |
| `H` | Hand tool |
| `Space` (hold) | Temporary hand tool |
| `Cmd` (hold) | Temporary hand tool |
| `Cmd+K` | Command Palette |
| `Cmd+Z` | Undo |
| `Cmd+Shift+Z` | Redo |
| `Home` | Center canvas |
| `Cmd+Scroll` | Zoom at cursor |

---

## 🔒 Безопасность (Open Source Ready)

### Принципы безопасности

1. **API ключи хранятся локально**
   - Ключ сохраняется только в IndexedDB браузера
   - Никогда не отправляется на сторонние серверы
   - Не логируется и не трекается

2. **Local-First архитектура**
   - Все данные хранятся на устройстве пользователя
   - Нет backend сервера (пока)
   - Нет телеметрии или аналитики

3. **Прямые API вызовы**
   - Запросы идут напрямую к OpenAI API
   - Без прокси-серверов
   - HTTPS шифрование

4. **Открытый код**
   - Весь код доступен для аудита
   - Нет обфускации или скрытой логики
   - MIT лицензия

### Рекомендации для пользователей

```bash
# Никогда не коммитьте API ключи
echo "*.env" >> .gitignore
echo ".env.local" >> .gitignore
```

---

## 💡 Преимущества системы

### Для исследователей и аналитиков
- **Визуализация альтернатив** — видите все варианты ответов одновременно
- **Пространственная память** — запоминаете где что находится
- **Быстрое сравнение** — ветвление позволяет A/B тестировать промпты

### Для промпт-инженеров
- **Итеративная разработка** — легко модифицировать и тестировать
- **История экспериментов** — все попытки сохраняются
- **Export для документации** — JSON экспорт для отчётов

### Для команд (будущее)
- **Shared workspaces** — совместная работа над исследованиями
- **Version control** — история изменений
- **Comments & annotations** — обсуждение прямо на графе

---

## 🚀 Roadmap

### Phase 1: Polish (Текущий) ✅
- [x] Полная система тем (Light/Dark)
- [x] Design Tokens
- [x] Minimap с zoom controls
- [x] Export/Import JSON
- [x] Undo/Redo
- [x] Enterprise UI (Lucide icons, custom selects)
- [x] Keyboard shortcuts
- [x] Node selection & hover states

### Phase 2: Enhanced UX (Q1 2025)
- [ ] **Multi-select** — выделение нескольких нод (Shift+Click)
- [ ] **Group operations** — перемещение, удаление группы
- [ ] **Smart connections** — bezier curves с routing
- [ ] **Node animations** — плавное появление и удаление
- [ ] **Search** — поиск по содержимому нод
- [ ] **Keyboard navigation** — Tab между нодами

### Phase 3: Advanced Features (Q2 2025)
- [ ] **Node types** — Text, Image, Code, Logic nodes
- [ ] **Templates** — сохранение и переиспользование структур
- [ ] **Auto-layout** — автоматическая организация графа
- [ ] **PDF/PNG export** — визуальный экспорт
- [ ] **Markdown support** — форматирование в нодах
- [ ] **Multiple AI providers** — Anthropic, Google, local models

### Phase 4: Collaboration (Q3 2025)
- [ ] **Backend service** — NestJS/Go для синхронизации
- [ ] **Real-time collaboration** — Yjs/CRDT
- [ ] **Authentication** — Clerk/Auth0
- [ ] **Team workspaces** — разделение по командам
- [ ] **Permissions** — роли и доступы
- [ ] **Comments** — обсуждение на графе

### Phase 5: AI Autonomy (Q4 2025)
- [ ] **Smart suggestions** — AI предлагает следующие шаги
- [ ] **Auto-branching** — автоматическое ветвление по теме
- [ ] **Vector search** — семантический поиск по истории
- [ ] **Agent mode** — автономное исследование темы

---

## 🏗️ Принципы разработки

### 1. Zero Latency
Интерфейс должен **летать**. Любое действие < 200ms.
- Оптимистичные обновления UI
- React.memo для предотвращения лишних рендеров
- Виртуализация для больших графов

### 2. Infinite Scale
Граф может быть **любого размера**.
- Виртуализация видимых нод
- Lazy loading контента
- Эффективные структуры данных

### 3. Data Ownership
Данные пользователя **принадлежат пользователю**.
- Local-first архитектура
- Полный экспорт в открытом формате
- Нет vendor lock-in

### 4. Enterprise Quality
Код должен быть **production-ready**.
- TypeScript strict mode
- FSD архитектура
- Comprehensive error handling
- Accessibility (a11y)

---

## 📁 Структура данных

### Node
```typescript
interface Node {
  id: string;
  x: number;
  y: number;
  prompt: string;           // Текущий запрос пользователя
  context: string;          // Контекст от родителя
  modelResponse: string;    // Ответ модели
  branchCount: 1 | 2 | 3 | 4;
  deepLevel: 1 | 2 | 3 | 4;
  isRoot: boolean;
  isPlaying: boolean;
  error?: string;
}
```

### Connection
```typescript
interface Connection {
  id: string;
  from: string;             // ID родительской ноды
  to: string;               // ID дочерней ноды
  fromPort: number;         // Индекс выходного порта
}
```

### Export Format
```typescript
interface ExportData {
  version: 1;
  timestamp: number;
  chatId: string;
  nodes: Node[];
  connections: Connection[];
  canvas?: CanvasState;
}
```

---

## 🤝 Contributing

Tension — open source проект. Мы приветствуем:
- Bug reports
- Feature requests
- Pull requests
- Documentation improvements

### Development
```bash
# Install dependencies
npm install

# Start dev server
npm run dev

# Type check
npm run typecheck

# Build
npm run build
```

---

## 📄 License

MIT License — свободное использование, модификация и распространение.

---

*Tension — где мысли обретают форму.*
